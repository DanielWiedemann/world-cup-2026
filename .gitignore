# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Node / Capacitor (added later)
node_modules/
package-lock.json
android/
ios/
www/

# Cloudflare Worker
worker/.wrangler/
worker/.dev.vars
worker/.admin-token
worker/dist/

# PWABuilder Android package (regenerated from PWA URL; keystore must be
# backed up SEPARATELY by the user — losing it means losing the ability
# to publish updates to Play Store)
android-package/

# Local
.claude/
